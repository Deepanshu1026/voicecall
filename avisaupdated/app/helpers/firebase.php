<?php
function send_firebase_notification($topicOrToken, $payload) {
    // Placeholder: implement actual Firebase HTTP v1 or legacy push later.
    // For now we just return true so code paths don't fail.
    return true;
}
